rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Uploads are handled by authenticated server routes through Firebase Admin.
    // Public file serving is done through generated Storage URLs, not client writes.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
